let profile = require('./profiles')
let role = require('./roles')
module.exports = {
    id: 'string',
    code: 'string',
    status: 'string',
    email: 'string',
    phone: 'string',
    profile: profile,
    role: role
}
